Commit all pending changes in this repo.

1. Run `git status --short` to see what will be committed
2. Show the user the file list and propose a conventional commit message (`feat:`, `fix:`, `chore:`, `docs:`)
3. Wait for the user to confirm the message before proceeding
4. Stage all changed files and commit with the confirmed message
