Review all uncommitted changes in this repo.

1. Run `git status --short` to list changed and untracked files
2. Run `git diff` for any modified tracked files
3. List untracked files and what they are

Summarise what changed and why it matters in plain language. Do not stage or commit anything.
